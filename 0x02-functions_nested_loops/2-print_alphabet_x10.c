#include "main.h"

/**
 * main - Entry point
 * Description: print lowercase alphabet 10 times
 * Return: Always 0 (success)
 */

void print_alphabet_x10(void)
{
	char v, w;
	
	while (v = 0; v < 10; v++);
	{
		while (w = 'a'; w <= 'z'; w++);
		{
			_putchar(w);
		}
		_putchar('\n');
	}
}
